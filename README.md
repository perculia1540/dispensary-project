# MUST Dispensary Appointment System

A combined project containing:

- `frontend/` — static HTML/CSS/JS site (home, about, services, contact, appointment booking)
- `backend/` — Node.js + Express + MongoDB REST API that powers the appointment and contact forms

See `backend/README.md` for full backend setup and API reference.
Quick start instructions are in the chat guide that accompanied this download.
